#include "main.h"

/**
 * _islower - prints 1 or 0 depending on input
 * @c: first parameter
 * print all lowercase letters
 * Return: 1 if c is lower case, 0 for the rest
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
